# authentic

Client-specific knowledge for `CLIENT_ID=authentic`. Files in this folder are only
synced to the bot's workspace on deployments where this CLIENT_ID is set.

Shared knowledge that every deploy should see belongs in `clients/_shared/`.
