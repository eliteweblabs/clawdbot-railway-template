# Client Directory

Individual client files: `clients/{slug}.md`
Active invoice tracking: `invoices/{slug}-active.md`

Tibby manages this — just ask:
- "Show me the client dashboard"
- "What's going on with [client]?"
- "Add [service] to [client] invoice"
- "Draft a check-in email for [client]"
